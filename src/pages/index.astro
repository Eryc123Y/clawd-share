---
import MainLayout from '../layouts/MainLayout.astro';
import { getCollection } from 'astro:content';

const news = (await getCollection('news'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const tutorials = (await getCollection('tutorials'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const research = (await getCollection('research'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 2);
---

<MainLayout title="分享前端开发与 AI 技术的智慧">
  <!-- Hero Section -->
  <section class="text-center py-16 mb-12">
    <h1 class="text-5xl font-bold text-gray-900 mb-6">
      分享前端开发与 AI 技术的智慧
    </h1>
    <p class="text-xl text-gray-600 max-w-2xl mx-auto">
      探索前沿技术，深度解析行业趋势，为您提供有价值的见解和实用资源。
    </p>
  </section>

  <!-- News Section -->
  <section id="news" class="mb-16">
    <div class="flex items-center gap-2 mb-6">
      <span class="text-2xl">📊</span>
      <h2 class="text-2xl font-bold text-gray-900">最新资讯</h2>
    </div>
    <div class="grid gap-6">
      {news.map((item) => (
        <article class="bg-white p-6 rounded-lg shadow-sm border border-gray-100 hover:shadow-md transition-shadow">
          <div class="flex items-start justify-between gap-4 mb-2">
            <h3 class="text-xl font-semibold text-gray-900">{item.data.title}</h3>
            <time class="text-sm text-gray-500 whitespace-nowrap">
              {item.data.date.toLocaleDateString('zh-CN')}
            </time>
          </div>
          <p class="text-gray-600">{item.data.summary}</p>
        </article>
      ))}
    </div>
  </section>

  <!-- Tutorials Section -->
  <section id="tutorials" class="mb-16">
    <div class="flex items-center gap-2 mb-6">
      <span class="text-2xl">📚</span>
      <h2 class="text-2xl font-bold text-gray-900">精选教程</h2>
    </div>
    <div class="grid gap-6 md:grid-cols-2">
      {tutorials.map((item) => (
        <article class="bg-white p-6 rounded-lg shadow-sm border border-gray-100 hover:shadow-md transition-shadow">
          <div class="flex items-start justify-between gap-4 mb-2">
            <h3 class="text-lg font-semibold text-gray-900">{item.data.title}</h3>
          </div>
          <p class="text-gray-600 mb-3">{item.data.summary}</p>
          <div class="flex items-center gap-3 text-sm">
            <span class="px-2 py-1 bg-blue-50 text-blue-700 rounded">
              {item.data.difficulty === 'beginner' && '初级'}
              {item.data.difficulty === 'intermediate' && '中级'}
              {item.data.difficulty === 'advanced' && '高级'}
            </span>
            {item.data.tags.map((tag) => (
              <span class="text-gray-500">#{tag}</span>
            ))}
          </div>
        </article>
      ))}
    </div>
  </section>

  <!-- Research Section -->
  <section id="research" class="mb-16">
    <div class="flex items-center gap-2 mb-6">
      <span class="text-2xl">🔬</span>
      <h2 class="text-2xl font-bold text-gray-900">深度研究</h2>
    </div>
    <div class="grid gap-6">
      {research.map((item) => (
        <article class="bg-gradient-to-r from-blue-50 to-purple-50 p-6 rounded-lg border border-blue-100">
          <div class="flex items-start justify-between gap-4 mb-3">
            <h3 class="text-xl font-semibold text-gray-900">{item.data.title}</h3>
            <time class="text-sm text-gray-500 whitespace-nowrap">
              {item.data.date.toLocaleDateString('zh-CN')}
            </time>
          </div>
          <p class="text-gray-700">{item.data.summary}</p>
        </article>
      ))}
    </div>
  </section>

  <!-- Resources Section -->
  <section id="resources" class="mb-16">
    <div class="flex items-center gap-2 mb-6">
      <span class="text-2xl">💎</span>
      <h2 class="text-2xl font-bold text-gray-900">推荐资源</h2>
    </div>
    <div class="grid gap-6 md:grid-cols-3">
      <div class="bg-white p-6 rounded-lg shadow-sm border border-gray-100">
        <h3 class="font-semibold text-gray-900 mb-4">📦 官方文档</h3>
        <ul class="space-y-2">
          <li><a href="https://react.dev" target="_blank" class="text-blue-600 hover:underline">React 官方文档</a></li>
          <li><a href="https://nextjs.org/docs" target="_blank" class="text-blue-600 hover:underline">Next.js 文档</a></li>
          <li><a href="https://developer.mozilla.org" target="_blank" class="text-blue-600 hover:underline">MDN 文档</a></li>
          <li><a href="https://github.com" target="_blank" class="text-blue-600 hover:underline">GitHub 文档</a></li>
        </ul>
      </div>
      <div class="bg-white p-6 rounded-lg shadow-sm border border-gray-100">
        <h3 class="font-semibold text-gray-900 mb-4">🛠️ 开发工具</h3>
        <ul class="space-y-2">
          <li><a href="https://vitejs.dev" target="_blank" class="text-blue-600 hover:underline">Vite</a></li>
          <li><a href="https://esbuild.dev" target="_blank" class="text-blue-600 hover:underline">esbuild</a></li>
          <li><a href="https://swc.rs" target="_blank" class="text-blue-600 hover:underline">SWC</a></li>
          <li><a href="https://rollupjs.org" target="_blank" class="text-blue-600 hover:underline">Rollup</a></li>
        </ul>
      </div>
      <div class="bg-white p-6 rounded-lg shadow-sm border border-gray-100">
        <h3 class="font-semibold text-gray-900 mb-4">🎨 设计资源</h3>
        <ul class="space-y-2">
          <li><a href="https://dribbble.com" target="_blank" class="text-blue-600 hover:underline">Dribbble</a></li>
          <li><a href="https://behance.net" target="_blank" class="text-blue-600 hover:underline">Behance</a></li>
          <li><a href="https://figma.com" target="_blank" class="text-blue-600 hover:underline">Figma</a></li>
          <li><a href="https://canva.com" target="_blank" class="text-blue-600 hover:underline">Canva</a></li>
        </ul>
      </div>
    </div>
  </section>

  <!-- Agent-Tracer Section -->
  <section class="bg-gradient-to-r from-gray-800 to-gray-900 text-white p-8 rounded-xl mb-16">
    <div class="flex items-center gap-2 mb-4">
      <span class="text-2xl">🔭</span>
      <h2 class="text-2xl font-bold">Agent-Tracer 深度研究</h2>
    </div>
    <p class="text-gray-300 mb-4">
      基于 agent-trace.dev 开发的智能体追踪平台，提供实时监控、分析和优化 AI Agent 的能力。
    </p>
    <div class="grid gap-3 mb-4">
      <div class="flex items-center gap-2">
        <span>📊</span>
        <span>实时追踪 - 监控 AI Agent 的行为和性能</span>
      </div>
      <div class="flex items-center gap-2">
        <span>🔍</span>
        <span>深度分析 - 分析 Agent 的决策过程和推理链</span>
      </div>
      <div class="flex items-center gap-2">
        <span>⚡</span>
        <span>优化建议 - 基于数据提供具体的优化建议</span>
      </div>
      <div class="flex items-center gap-2">
        <span>📝</span>
        <span>可视化报告 - 生成交互式的分析报告</span>
      </div>
    </div>
    <div class="flex flex-wrap gap-4 text-sm text-gray-400">
      <span>研究日期：2026-01-28</span>
      <span>应用场景：Agent 调试、性能基准测试、工作流优化</span>
    </div>
  </section>
</MainLayout>

---
import MainLayout from '../layouts/MainLayout.astro';
import { getCollection } from 'astro:content';

// 按分类获取资讯（目前为空）
const allNews = await getCollection('news');
const categories = ['学术前沿', '产业界', '开源社区', '财经信息'];

const newsByCategory = categories.map((category) => {
  return {
    category,
    items: allNews
      .filter((item) => item.data.category === category)
      .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
      .slice(0, 4),
  };
});
---

<MainLayout title="每日 AI 资讯">
  <style>
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(60px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes slideLeft {
      from {
        opacity: 0;
        transform: translateX(-40px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes pulse {
      0%, 100% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.05);
      }
    }

    .hero {
      min-height: 85vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 4rem 2rem;
      position: relative;
      overflow: hidden;
      background: linear-gradient(135deg, #FEF9E7 0%, #2EC4B6 20%, #FEF9E7 40%);
      background-size: 200% 200%;
    }

    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%232EC4B6' fill-opacity='0.04'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/svg%3E");
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.75rem 1.5rem;
      background: #2EC4B6;
      color: white;
      font-family: 'Space Mono', monospace;
      font-size: 0.75rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      border-radius: 0;
      margin-bottom: 1.5rem;
      animation: slideLeft 0.8s ease-out both;
    }

    .hero h1 {
      font-size: clamp(2.5rem, 7vw, 5rem);
      margin-bottom: 1.5rem;
      color: #1A1A2E;
      animation: fadeInUp 1s ease-out 0.2s both;
    }

    .hero p {
      font-size: 1.25rem;
      color: #1A1A2E;
      max-width: 600px;
      line-height: 1.8;
      margin-bottom: 2rem;
      animation: fadeInUp 1s ease-out 0.4s both;
    }

    .category-section {
      margin-bottom: 4rem;
    }

    .category-header {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 3px solid #1A1A2E;
      animation: fadeInUp 0.8s ease-out both;
    }

    .category-icon {
      font-size: 2rem;
      width: 3.5rem;
      height: 3.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
      background: white;
      border: 3px solid #1A1A2E;
      font-weight: 700;
      border-radius: 50%;
    }

    .category-header h2 {
      margin: 0;
      font-size: 2rem;
      font-family: 'Playfair Display', Georgia, serif;
      color: #1A1A2E;
    }

    .category-学术前沿 .category-icon {
      background: #FF6B35;
      color: white;
      border-color: #FF6B35;
    }

    .category-产业界 .category-icon {
      background: #2EC4B6;
      color: white;
      border-color: #2EC4B6;
    }

    .category-开源社区 .category-icon {
      background: #F7C59F;
      color: white;
      border-color: #F7C59F;
    }

    .category-财经信息 .category-icon {
      background: #1A1A2E;
      color: white;
      border-color: #1A1A2E;
    }

    .news-grid {
      display: grid;
      gap: 1.5rem;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    }

    .news-card {
      animation: fadeInUp 0.6s ease-out both;
      position: relative;
    }

    .news-card a {
      display: block;
      color: inherit;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .news-card a:hover {
      transform: translateY(-4px) rotate(0.5deg);
    }

    .source-info {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 2px solid rgba(26, 26, 46, 0.1);
      font-size: 0.875rem;
      color: #666;
      font-family: 'Space Mono', monospace;
    }

    .source-icon {
      font-size: 1rem;
    }

    .read-more {
      display: inline-flex;
      align-items: center;
      gap: 0.25rem;
      margin-left: auto;
      padding: 0.5rem 1rem;
      background: #1A1A2E;
      color: #FEF9E7;
      border-radius: 0;
      font-size: 0.75rem;
      font-weight: 500;
      transition: all 0.3s ease;
    }

    .news-card a:hover .read-more {
      background: #2EC4B6;
      color: white;
    }

    .empty-state {
      text-align: center;
      padding: 4rem 2rem;
      background: white;
      border: 2px dashed #1A1A2E;
      border-radius: 0;
    }

    .empty-state h3 {
      font-family: 'Playfair Display', Georgia, serif;
      font-size: 1.5rem;
      margin-bottom: 1rem;
      color: #1A1A2E;
    }

    .empty-state p {
      color: #666;
      line-height: 1.7;
      font-size: 1rem;
    }

    .empty-state .emoji {
      font-size: 4rem;
      margin-bottom: 1.5rem;
      display: block;
    }
  </style>

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-badge">
      📊 四大板块
    </div>
    <h1>每日 AI 资讯</h1>
    <p>学术前沿 · 产业界 · 开源社区 · 财经信息</p>
  </section>

  <!-- News Categories -->
  {newsByCategory.map(({ category, items }) => (
    <section class="category-section category-{category}">
      <div class="category-header">
        {category === '学术前沿' && <span class="category-icon">📚</span>}
        {category === '产业界' && <span class="category-icon">🏭</span>}
        {category === '开源社区' && <span class="category-icon">🌍</span>}
        {category === '财经信息' && <span class="category-icon">💰</span>}
        <h2>{category}</h2>
      </div>
      
      {items.length === 0 ? (
        <div class="empty-state">
          <span class="emoji">📭</span>
          <h3>暂无内容</h3>
          <p>{category} 板块的资讯正在准备中，敬请期待。</p>
        </div>
      ) : (
        <div class="news-grid">
          {items.map((item, index) => (
            <article class="card news-card" style="animation-delay: {index * 0.15}s">
              <a href={item.data.sourceUrl} target="_blank" rel="noopener noreferrer">
                <time style="font-family: 'Space Mono', monospace; font-size: 0.875rem; color: #666; display: block; margin-bottom: 0.75rem;">
                  {item.data.date.toLocaleDateString('zh-CN')}
                  {item.data.readTime && ` · ${item.data.readTime}` }
                </time>
                <h3 style="font-size: 1.15rem; margin-bottom: 0.5rem;">{item.data.title}</h3>
                <p style="color: #666; line-height: 1.7; font-size: 0.9rem;">{item.data.summary}</p>
                <div class="tags">
                  {item.data.tags.map((tag) => <span class="tag">#{tag}</span>)}
                </div>
                <div class="source-info">
                  <span class="source-icon">🔗</span>
                  <span>{item.data.sourceName || '原始链接'}</span>
                  <span class="read-more">阅读原文 →</span>
                </div>
              </a>
            </article>
          ))}
        </div>
      )}
    </section>
  ))}
</MainLayout>

---
import MainLayout from '../layouts/MainLayout.astro';
import { getCollection } from 'astro:content';

const news = (await getCollection('news'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 6);

const tutorials = (await getCollection('tutorials'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const research = (await getCollection('research'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 2);
---

<MainLayout title="每日 AI 资讯">
  <style>
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(60px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0.8);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    @keyframes pulse {
      0%, 100% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.05);
      }
    }

    @keyframes slideLeft {
      from {
        opacity: 0;
        transform: translateX(-40px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    .hero {
      min-height: 85vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 4rem 2rem;
      position: relative;
      overflow: hidden;
      background: linear-gradient(135deg, #FEF9E7 0%, #F7C59F 50%, #FEF9E7 100%);
      background-size: 200% 200%;
    }

    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23FF6B35' fill-opacity='0.04'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
      }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.75rem 1.5rem;
      background: #FF6B35;
      color: white;
      font-family: 'Space Mono', monospace;
      font-size: 0.75rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.2em;
      border-radius: 0;
      margin-bottom: 1.5rem;
      animation: slideLeft 0.8s ease-out both;
    }

    .hero h1 {
      font-size: clamp(2.5rem, 7vw, 5rem);
      margin-bottom: 1.5rem;
      color: #1A1A2E;
      animation: fadeInUp 1s ease-out 0.2s both;
    }

    .hero p {
      font-size: 1.25rem;
      color: #1A1A2E;
      max-width: 600px;
      line-height: 1.8;
      margin-bottom: 2rem;
      animation: fadeInUp 1s ease-out 0.4s both;
    }

    .daily-highlight {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1rem;
      background: white;
      border: 2px solid #2EC4B6;
      color: #1A1A2E;
      font-family: 'Space Grotesk', sans-serif;
      font-size: 0.875rem;
      font-weight: 600;
      animation: scaleIn 0.6s ease-out 0.8s both;
    }

    .section-header {
      display: flex;
      align-items: baseline;
      gap: 1rem;
      margin-bottom: 3rem;
      flex-wrap: wrap;
    }

    .section-header h2 {
      margin: 0;
      position: relative;
      display: inline;
    }

    .section-header h2::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 80px;
      height: 4px;
      background: #FF6B35;
    }

    .section-count {
      font-family: 'Space Mono', monospace;
      font-size: 3rem;
      font-weight: 700;
      color: #FF6B35;
      line-height: 1;
    }

    .news-grid {
      display: grid;
      gap: 1.5rem;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    }

    .news-card {
      animation: fadeInUp 0.8s ease-out both;
    }

    .news-card:nth-child(1) { animation-delay: 0.1s; }
    .news-card:nth-child(2) { animation-delay: 0.2s; }
    .news-card:nth-child(3) { animation-delay: 0.3s; }
    .news-card:nth-child(4) { animation-delay: 0.4s; }
    .news-card:nth-child(5) { animation-delay: 0.5s; }
    .news-card:nth-child(6) { animation-delay: 0.6s; }

    .tutorial-grid {
      display: grid;
      gap: 1.5rem;
      grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    }

    .tutorial-card {
      animation: fadeInUp 0.8s ease-out both;
    }

    .tutorial-card:nth-child(1) { animation-delay: 0.7s; }
    .tutorial-card:nth-child(2) { animation-delay: 0.8s; }
    .tutorial-card:nth-child(3) { animation-delay: 0.9s; }

    .research-grid {
      display: grid;
      gap: 1.5rem;
    }

    .research-card {
      animation: fadeInUp 0.8s ease-out both;
    }

    .research-card:nth-child(1) { animation-delay: 1s; }
    .research-card:nth-child(2) { animation-delay: 1.1s; }

    .difficulty-badge {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      font-family: 'Space Mono', monospace;
      font-size: 0.75rem;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      border: 2px solid;
      background: transparent;
    }

    .difficulty-beginner {
      border-color: #2EC4B6;
      color: #2EC4B6;
    }

    .difficulty-intermediate {
      border-color: #FF6B35;
      color: #FF6B35;
    }

    .difficulty-advanced {
      border-color: #F7C59F;
      color: #F7C59F;
    }

    .tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-top: 1rem;
      font-family: 'Space Mono', monospace;
      font-size: 0.75rem;
    }

    .tag {
      padding: 0.25rem 0.75rem;
      background: #1A1A2E;
      color: #FEF9E7;
      border-radius: 0;
    }

    .update-notice {
      text-align: center;
      padding: 3rem 2rem;
      background: linear-gradient(135deg, #2EC4B6 0%, #CBF3F0 100%);
      color: #1A1A2E;
      border: 3px solid #1A1A2E;
      margin-bottom: 4rem;
    }

    .update-notice h2 {
      font-family: 'Playfair Display', Georgia, serif;
      font-size: 2rem;
      margin-bottom: 1rem;
    }

    .update-notice p {
      font-size: 1.1rem;
      max-width: 600px;
      margin: 0 auto;
    }
  </style>

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-badge">
      ⚡ 每日更新
    </div>
    <h1>AI 资讯日报</h1>
    <p>每天一条，精选前沿动态<br/>紧跟 AI 时代脉搏</p>
    <div class="daily-highlight">
      🤖 AI · 🚀 技术 · 📊 分析
    </div>
  </section>

  <!-- Update Notice -->
  <section>
    <div class="update-notice">
      <h2>📅 每日精选 · 专注前沿</h2>
      <p>我们每天为您精选 3-5 条最重要的 AI 技术动态，包括大模型更新、行业新闻、开源项目、工具推荐等。让您不错过任何重要资讯。</p>
    </div>
  </section>

  <!-- News Section -->
  <section id="news">
    <div class="section-header">
      <span class="section-count">01</span>
      <h2>今日资讯</h2>
    </div>
    <div class="news-grid">
      {news.map((item, index) => (
        <article class="card news-card">
          <time style="font-family: 'Space Mono', monospace; font-size: 0.875rem; color: #666; display: block; margin-bottom: 1rem;">
            {item.data.date.toLocaleDateString('zh-CN')}
          </time>
          <h3 style="font-size: 1.25rem; margin-bottom: 0.75rem;">{item.data.title}</h3>
          <p style="color: #666; line-height: 1.7; font-size: 0.95rem;">{item.data.summary}</p>
          <div class="tags">
            {item.data.tags.map((tag) => <span class="tag">#{tag}</span>)}
          </div>
        </article>
      ))}
    </div>
  </section>

  <!-- Tutorials Section -->
  <section id="tutorials">
    <div class="section-header">
      <span class="section-count">02</span>
      <h2>深度教程</h2>
    </div>
    <div class="tutorial-grid">
      {tutorials.map((item, index) => (
        <article class="card tutorial-card">
          <h3 style="font-size: 1.25rem; margin-bottom: 0.75rem;">{item.data.title}</h3>
          <p style="color: #666; line-height: 1.7; font-size: 0.95rem; margin-bottom: 1rem;">{item.data.summary}</p>
          <div style="display: flex; gap: 0.75rem; align-items: center; flex-wrap: wrap;">
            <span class={`difficulty-badge difficulty-${item.data.difficulty}`}>
              {item.data.difficulty === 'beginner' && '初级'}
              {item.data.difficulty === 'intermediate' && '中级'}
              {item.data.difficulty === 'advanced' && '高级'}
            </span>
            <div class="tags">
              {item.data.tags.map((tag) => <span class="tag">#{tag}</span>)}
            </div>
          </div>
        </article>
      ))}
    </div>
  </section>

  <!-- Research Section -->
  <section id="research">
    <div class="section-header">
      <span class="section-count">03</span>
      <h2>研究报告</h2>
    </div>
    <div class="research-grid">
      {research.map((item, index) => (
        <article class="card research-card" style="background: linear-gradient(135deg, #FEF9E7 0%, #F7C59F 20%, #FEF9E7 40%); border-color: #FF6B35;">
          <div style="display: flex; justify-content: space-between; align-items: flex-start; gap: 1rem; margin-bottom: 1rem;">
            <h3 style="font-size: 1.5rem;">{item.data.title}</h3>
            <time style="font-family: 'Space Mono', monospace; font-size: 0.875rem; white-space: nowrap; color: #666;">
              {item.data.date.toLocaleDateString('zh-CN')}
            </time>
          </div>
          <p style="color: #666; line-height: 1.7; margin-bottom: 1rem; font-size: 0.95rem;">{item.data.summary}</p>
          <div class="tags">
            {item.data.tags.map((tag) => <span class="tag" style="background: #FF6B35; color: white;">#{tag}</span>)}
          </div>
        </article>
      ))}
    </div>
  </section>
</MainLayout>
